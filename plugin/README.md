# Plugin Directory

This folder contains the compiled RhinoClaw plugin (`rhinoclaw.rhp`).

## How to populate

Run the release script from the **development** repository:

```bash
cd ~/projects/RhinoClaw
./scripts/release.sh
```

This will copy the latest build output here.

If you downloaded this from GitHub Releases, the `.rhp` file should already be included.
